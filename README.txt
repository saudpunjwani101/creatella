Run the server by doing "npm start" inside the server directory

To run the app you'll need go to the createalla directory, do "npm install"
to install all the dependencies, and then run the app through react-native cmd.
